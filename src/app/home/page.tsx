import HomeClient from "./HomeClient";

export const metadata = {
  title: "FocuxAI Engine™ — Sistema Operativo para Constructoras",
  description: "Plataforma de transformación digital con IA para el sector construcción.",
};

export default function HomePage() {
  return <HomeClient />;
}
